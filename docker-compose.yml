version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - SECRET_KEY=phantom-admin-secret-key-change-in-production
      - FLASK_ENV=production
    volumes:
      - dbdata:/data
      - .:/app
    restart: unless-stopped

volumes:
  dbdata: